doom โครงสร้าง แม่ลูก บลาๆ

Synchronous  - ทำคำสั่งนึงให้เสร็จ อีกคำสั่งค่อยทำ

Asynchronous - ทำไปเลย ไม่รอ ไปทำคำสั่งต่อไปเลย อันไหนไม่เสร็จ ก็ไปอีกคำสั่ง   

hihher order function คือฟังชัน ที่รับ ฟังชันมาเป็นพารามิเตอร์
callback function คือ ฟังชันที่เป็นพารามิเตอร์ 


print = console.log 

data type 2 กลุ่มใหญ่ๆ

primitive
-number ทศนิยม เต็ม ได้หมดด
-String
-boolean

object type
have value name 
array
null

null and undefined

ถ้าใช้ null = รู้แน่ชัด ว่าเดี้ยวจะมี value 
underfined = ใช้เมื่ออาจจะมีเหตุการณ์ที่อาจจะมี value แต่ไม่มี คาดเดาไม่ได้ว่าจะมีค่าไหม 

var re-declared ได้ ( ประกาศอีกรอบ)

let นอกฟังชันก์ได้ ในฟังก์ชันได้  ประกาศข้างนอกได้  ถ้าไปอยู่ในฟังชัน ก็ใช้ได้แค่ในฟังชัน์นั้น update ได้อย่างเดียว

const update , re declared ไม่ได้  เหมือน let ที่ อัปเดตไม่ได้ 